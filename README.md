# Custom Secure Bootloader demo for ESP32-C3 (RISC-V)

A custom implementation of a cryptographic chain of trust on the ESP32-C3 RISC-V microcontroller. This project demonstrates a secure boot flow by manually implementing image signing, header generation, and verification logic on the device, rather than relying on the default Espressif Secure Boot V2 hardware abstraction.
